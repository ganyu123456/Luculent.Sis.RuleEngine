# 项目验收文档

> 版本: v1.4.1 | 更新: 2026-07-19

## 验收环境

| 项目 | 说明 |
|------|------|
| 部署方式 | Docker Compose 本地部署 |
| Redis | `ruleengine-redis` (redis:7-alpine) |
| ClickHouse | `ruleengine-clickhouse` (clickhouse/clickhouse-server:24-alpine) |
| Master | `ruleengine-master` (端口 11082/11083) |
| Worker | `03-luculentsisruleengine-worker-1`, `-2` (默认 2 副本) |
| Monitor Center | LiEMS 主应用 (PostgreSQL + ABP) |
| TrendDB | `10.181.0.59:20100` / `db05.test1` (唯一实时测点) |
| 代码版本 | v1.4.0 (77 单元测试全部通过, CI 绿) |

---

## 一、单元测试验收

### 1.1 运行命令

```bash
cd 03-Luculent.Sis.RuleEngine
dotnet test Luculent.Sis.RuleEngine.sln
```

### 1.2 测试清单 (77 项全部通过)

#### 前置规则评估 (19 tests)
- `EvaluateRangeDuration_BreakOnHitTrue_ReturnsTrueImmediately`
- `EvaluateRangeDuration_BreakOnHitFalse_ReturnsTrueAfterDuration`
- `EvaluateRangeDuration_NoMatchingCondition_ReturnsFalse`
- `EvaluateRangeDuration_EmptyRules_ReturnsFalse`
- `EvaluateRangeDuration_DisabledRule_Skipped`
- `EvaluateRangeDuration_MultipleRules_FirstHitBreaksWithBreakOnHit`
- `EvaluateExpression_ValidComparison_ReturnsTrue`
- `EvaluateExpression_InvalidComparison_ReturnsFalse`
- `EvaluateExpression_NoExpression_ReturnsFalse`
- `EvaluateExpression_AndComposition_ReturnsTrue`
- `EvaluateExpression_OrComposition_ReturnsCorrect`
- `EvaluateExpression_EqualityCheck_ReturnsCorrect`
- `EvaluateAllAsync_NotEnabled_ReturnsTrue`
- `EvaluateAllAsync_UnknownRuleType_ReturnsTrue`
- `EvaluateAllAsync_UpdatesAllStates`
- `EvaluateAllAsync_EmptyDefinitions_NoOp`
- `FetchSourceData_StaticSource_ParsesSourceKeyDirectly`
- `FetchSourceData_RealDB_UsesSourceKeyAsTagName`
- `FetchSourceData_RealDBFailure_ReturnsFalse`

#### Worker 计算流程 (7 tests)
- `ProcessMonitor_StateChange_WritesHistoryAndSavesState`
- `ProcessMonitor_SameStatus_NoDuplicateHistory`
- `ProcessMonitor_ClearEvent_WritesEmptyStatusAndClearsRealtime`
- `ProcessMonitor_AfterClear_NoDuplicateEventNextCycle`
- `ProcessMonitor_PereruleSuppressWithClear_WritesEmptyStatus`
- `ProcessMonitor_PereruleSuppressNoClear_NoEvent`
- `ProcessMonitor_FirstCalculation_NoSpuriousEvent`

#### 前置规则 Pipeline (15 tests)
- `CheckAsync_*` — 覆盖 ManualFlag、StopMonitor、SourceDependency、InterfaceMonitoring 等全部抑制路径

#### 规则计算器 (25 tests)
- CalculateRuleRangeDuration (4), CalculateRuleRangeFrequency (2), CalculateRuleExpression (2)
- CalculateFeatureValue (5), CalculatePackageValue (5), CalculateRulePackageValue (2)
- CalculateInterfaceMonitoring (5)

#### 分区算法 (6 tests)
- Cost-Aware 贪心装箱：均衡性、成本计算、单Worker、空输入、空Worker

#### 性能测试 (4 tests)
- Case1: 100K 监视项单周期 — 平均 < 500ms, 100,000 事件
- Case2: 100K 监视项 5 分钟持续 — 吞吐 333 events/s, 内存增长 < 1GB
- Case3: 多 Worker 并行扩展 — 1W=315ms, 2W=187ms, 4W=162ms
- Case4: 前置规则开销 — 10K 监视项, 13.2% 开销

#### TrendDB 读取 (1 test)
- `Read_Single_Tag_dbtest1`

### 1.3 预期结果

```
已通过! - 失败: 0, 通过: 77, 已跳过: 0, 总计: 77
```

### 1.4 性能基准

| 指标 | 值 |
|------|-----|
| 单周期平均耗时 (100K) | 255.3ms |
| P99 延迟 | 686.0ms |
| 5分钟持续吞吐 | 333 events/s |
| 多Worker扩展性 | 1W=315ms → 4W=162ms |
| 前置规则开销 (10K) | 13.2% |
| 5分钟内存 | 774MB → 296MB (GC回收后稳定) |

---

## 二、异常场景测试验收

> 详细测试步骤见 `docs/异常场景测试手册.md`

规则引擎作为生产环境核心组件，必须验证以下异常场景的自动恢复能力。每次发版前需执行完整回归测试。

### 2.1 测试场景清单

| 场景 | 操作 | 核心验收标准 |
|------|------|-------------|
| 场景 1: Worker 重启 | `docker restart` 单个 Worker | Worker 30s 内恢复，事件继续写入，lastEventName 恢复 |
| 场景 1b: 全部 Worker 重启 | 同时重启所有 Worker | 60s 内全部恢复，无死 Worker 误判 |
| 场景 2: Master 重启 | `docker restart ruleengine-master` | API < 15s 恢复，Worker 60s 内重连，事件无中断 |
| 场景 2b: Master 长时间宕机 | `docker stop` Master 2 分钟 | 宕机期间 Worker 继续计算，恢复后重连正常 |
| 场景 3: SIS 服务重启 | `docker restart sis-service` | MonitorCenter API 恢复 < 30s，EvnetData/History 查询正常 |
| 场景 4: Worker 扩容 | `--scale worker=2→3` | 新 Worker 30s 内注册，重分区触发，监视项均匀分配 |
| 场景 4b: Worker 缩容 | `--scale worker=3→2` | 死 Worker 30s 检测，45s 内重分区，监视项总数不变 |
| 场景 5: ClickHouse 重启 | `docker restart` ClickHouse | 恢复 < 10s，Worker 写入恢复，事件总数不减少 |
| 场景 5b: Redis 重启 | `docker restart` Redis | 实时报警重建，核心计算不受影响 |
| 场景 6: 配置变更 | MonitorCenter 推送变更 | 健康检查正常，实时报警无异常 |

### 2.2 数据一致性验证

| 检查项 | containNull | 期望 |
|--------|-------------|------|
| EvnetData 空状态事件 | true | > 0 (有空状态事件) |
| EvnetData 空状态事件 | false | = 0 (全部过滤) |
| HistoryMonitor 负时长 | true / false | = 0 |
| lastEventName | 两者 | 非空 (第二个事件起) |
| EvnetData totalCount | true vs false | true > false |
| RealMonitor ShowAll=true | — | ≥ 总监视项数 |
| RealMonitor ShowAll=false | — | > 0 (仅报警项) |
| **事件交替** | — | **同一监视项相邻事件 statusKey 互不相同 (0 行异常)** |
| **历史会话时长** | — | **所有闭合会话 endTime ≥ startTime (0 行负时长)** |

### 2.3 回归测试脚本

CI/CD 集成两套脚本，路径见 `docs/异常场景测试手册.md`:

| 脚本 | 用途 | 运行时机 | 耗时 |
|------|------|----------|------|
| `ci_smoke_test.sh` | 健康检查 + 实时/历史查询 + Worker 心跳 | 每次部署前 | 2-3 分钟 |
| `ci_regression_test.sh` | Worker 重启恢复 + containNull 一致性 + RealMonitor + 单元测试 + 事件交替自检 | 发版前 | 10-15 分钟 |

### 2.4 已知限制

1. **InMemoryStateStore**: Worker 重启后状态丢失，需从 ClickHouse 恢复。生产环境应迁移至 RocksDB
2. **首次恢复事件 lastEventId**: 为 null (仅恢复 statusKey)
3. **ClickHouse 写入队列满**: 丢弃最旧事件 (BoundedChannelFullMode.DropOldest)
4. **Master 单点**: 当前无 Master 高可用方案

---

## 三、规则覆盖验收

### 2.1 规则引擎支持的规则类型

对标 `MonitorCenter/Managers/Rule` 目录：

| RuleType | 规则类 | 对应 Calculator | 状态 |
|----------|--------|----------------|------|
| Expression (0) | 表达式规则 | `CalculateRuleExpression` | 已实现 |
| RangeDuration (2) | 区间时长规则 | `CalculateRuleRangeDuration` | 已实现 |
| RangeFrequency (3) | 区间频率规则 | `CalculateRuleRangeFrequency` | 已实现 |
| PackageValue (4) | 打包值规则 | `CalculatePackageValue` | 已实现 |
| FeatureValue (5) | 特征值规则 | `CalculateFeatureValue` | 已实现 |
| WallTemperature (6) | 壁温规则 | `CalculateWallTemperature` | 已实现 |
| InterfaceMonitoring (7) | 接口监视 | `CalculateInterfaceMonitoring` | 已实现 |
| RulePackageValue (8) | 规则打包值 | `CalculateRulePackageValue` | 已实现 |
| RuleMultiStateRangeDuration (9) | 多状态区间时长 | `CalculateMultiStateRangeDuration` | 已实现 |

### 2.2 前置规则

对标 `MonitorCenter/Managers/Prerule`：

| 前置规则 | 说明 | 状态 |
|----------|------|------|
| ManualFlag 检查 | 手动停止标志 | 已实现 |
| StopMonitor 关联启停 | 关联监视项处于报警状态时抑制 | 已实现 |
| SourceDependency 数据源依赖 | 数据源关联监视项不可用时抑制 | 已实现 |
| Expression 前置规则 | 表达式条件判断 (如 tag_value > threshold) | 已实现 |
| RangeDuration 前置规则 | 区间时长条件 (如 tag_value >= threshold 持续 N 秒) | 已实现 |
| 数据库 Fallback | MonitorCenter API 不可用时 Npgsql 直读 PostgreSQL | 已实现 |
| Admin API 推送 | `/api/ruleengine/admin/load-prerules` 加载并推送到 Workers | 已实现 |

---

## 四、数据正确性验证 (核心)

计算引擎的核心不变量和验证方法。任何违反以下规则的现象均表明计算引擎或数据流存在 bug。

### 4.1 事件交替不变量

**规则：同一监视项的相邻事件，`statusKey` 必须不同。**

计算引擎只在 `newStatus != PreviousStatus` 时才写入事件（`WorkerCalculationService.ProcessMonitorAsync:281`）。因此，对于同一监视项，按 `occurTime` 升序排列的事件序列中，相邻两行的 `statusKey` 绝不相等。

违反此规则的可能原因：
- Worker 重启后状态恢复错误，导致重复写入同一状态
- 多 Worker 同时处理同一监视项（分区错误）
- ClickHouse 写入重复（缺少去重）

**验证 SQL (ClickHouse)：**

```sql
-- 检测相邻事件 statusKey 相同的异常 (同一监视项内)
-- 注意: 排除每个 partition 的第一行，避免 lagInFrame 默认值 "" 与空状态事件误匹配
SELECT monitor_id, monitor_key, occur_time, status_key, prev_status_key
FROM (
    SELECT monitor_id, monitor_key, occur_time, status_key,
           lagInFrame(status_key) OVER (PARTITION BY monitor_id ORDER BY occur_time) AS prev_status_key,
           row_number() OVER (PARTITION BY monitor_id ORDER BY occur_time) AS rn
    FROM ruleengine.alarm_events
    WHERE occur_time >= now() - INTERVAL 1 HOUR
)
WHERE rn > 1 AND status_key = prev_status_key
LIMIT 10;

-- 预期: 0 行。如果返回结果 > 0，表示计算引擎存在重复写入 bug
```

**前端对应页面:** 事件列表 (`event-list`) — 同一监视项的事件应按时间轴交替出现，不应连续出现相同状态。

### 4.2 历史会话完整性

**规则：每条历史记录是一个完整的告警会话，包含开始时间、结束时间、持续时长、极值。**

一次完整的告警生命周期由一个"有状态事件" (`statusKey != ""`) 和一个紧随其后的"无状态事件" (`statusKey = ""`) 配对而成：

| 属性 | 来源 |
|------|------|
| `startTime` | 有状态事件的 `occurTime` |
| `endTime` | 紧随其后的无状态事件的 `occurTime` |
| `Duration` | `endTime - startTime`，必须 ≥ 0 |
| `maxValue` / `minValue` | 告警期间采集到的极值 |

对于仍未消除的活跃告警，`endTime` 为空，`Duration` 为 `null`。

**验收标准：**

- [ ] 每条历史记录的 `endTime` ≥ `startTime`（持续时长为非负数）
- [ ] 同一监视项的 `startTime` 按时间升序排列，相邻记录不重叠
- [ ] `maxValue` / `minValue` 反映告警期间的极值，不应始终等于 `triggerValue`
- [ ] 活跃告警 (`endTime` 为空) 同时存在于 Redis 实时报警中

**验证 SQL (ClickHouse)：**

```sql
-- 负时长检测: startTime > endTime
SELECT monitor_id, monitor_key, start_time, end_time,
       (end_time - start_time) / 1000 AS duration_sec
FROM (
    -- 事件流配对: 有状态事件 + 下一个无状态事件
    SELECT monitor_id, monitor_key, occur_time AS start_time, status_key,
           leadInFrame(occur_time) OVER (PARTITION BY monitor_id ORDER BY occur_time) AS end_time,
           leadInFrame(status_key) OVER (PARTITION BY monitor_id ORDER BY occur_time) AS next_status_key
    FROM ruleengine.alarm_events
    WHERE occur_time >= now() - INTERVAL 1 HOUR
)
WHERE status_key != '' AND (next_status_key = '' OR next_status_key IS NULL)
  AND end_time IS NOT NULL
  AND end_time > '2000-01-01'
  AND end_time < start_time
LIMIT 10;

-- 预期: 0 行。负时长表示事件配对逻辑或时间戳写入异常
```

**前端对应页面:** 历史列表 (`history-list`) — 每条记录应显示非负的持续时长。

### 4.3 数据流

```
TrendDB (db05.test1) ──→ DataAcquisitionService (1s 采集)
                              ↓
                        TagValueStore
                              ↓
                    WorkerCalculationService (1s 计算)
                      newStatus != PreviousStatus ? → 写入事件
                         ↓          ↓
                   Redis (实时)   ClickHouse (历史)
                         ↓          ↓
                   /realtime API  /history API
                         ↓          ↓
                   MonitorCenter 前端页面
```

### 4.4 闭环完整性

```bash
# 查询闭环状态 (GET 请求，固定查最近 7 天)
curl http://localhost:11082/api/ruleengine/alarms/history/closed-loop/validate
```

**验收标准：**

- [ ] 有状态事件和无状态事件数量匹配（闭环完整）
- [ ] 不产生连续重复的同状态事件（事件交替不变量）
- [ ] 每条闭合的会话 `endTime ≥ startTime`

---

## 五、接口查询验收

### 5.1 实时报警查询

```bash
# 全量实时报警
curl http://localhost:11082/api/ruleengine/alarms/realtime

# 指定监视项
curl "http://localhost:11082/api/ruleengine/alarms/realtime?monitorIds=mon-1"
```

**验收标准：**
- [x] 返回全量实时报警列表
- [x] 每条报警包含: `monitorId`, `monitorKey`, `monitorName`, `statusKey`, `statusName`, `value`, `occurTime`, `workerId`
- [x] 支持按 `monitorIds` 过滤

### 5.2 历史报警查询

```bash
# 分页查询
curl -X POST http://localhost:11082/api/ruleengine/alarms/history \
  -H "Content-Type: application/json" \
  -d '{
    "monitorKeys": ["MON-0000001"],
    "startTime": "2026-07-01T00:00:00",
    "endTime": "2026-07-19T00:00:00",
    "eventTypes": ["trigger"],
    "skipCount": 0,
    "maxResultCount": 100
  }'
```

**验收标准：**
- [x] 支持时间范围过滤 (`startTime`/`endTime`)
- [x] 支持事件类型过滤 (`eventTypes`: "trigger"/"clear")
- [x] 支持监视项过滤 (`monitorIds` / `monitorKeys`)
- [x] 支持状态键过滤 (`statusKeys`)
- [x] 分页支持 (`skipCount`/`maxResultCount`)
- [x] 返回包含: `lastEventId`, `lastEventName`, `unit`, `jobId` 新字段

### 5.3 闭环验证

```bash
curl "http://localhost:11082/api/ruleengine/alarms/history/closed-loop/validate"
```

**验收标准：**
- [x] 返回触发/消除总数统计
- [x] 返回未关闭事件详情 (`OpenEvents`)

---

## 六、前端页面验收

### 5.1 事件列表 (event-list)

URL: `http://127.0.0.1:5501/app/sis/monitor-center/event-list/`

**数据正确性规则:** 见 [4.1 事件交替不变量](#41-事件交替不变量)。选择同一监视项，事件列表中的 `statusKey` 应交替变化，绝不应出现连续两行相同状态。

| 字段 | 说明 | 状态 |
|------|------|------|
| `relatedStatusName` | 关联主题类型 (MonitorStatus.Name) | 已修复 — 分布式模式从 DB 查询 MonitorStatus |
| `occurMonitorStatusName` | 事件状态 (MonitorStatusDefinition.Name) | 已修复 — 分布式模式从 DB 查询定义 |
| `occurValue` | 事件值 | 已支持 |
| `lastEventId` / `lastEventName` | 事件前状态 | 已支持 |
| `unit` / `jobId` | 单位/任务ID | 已支持 |

检索条件验收：
- [x] filter (监视项名称/编码模糊搜索)
- [x] monitorItemIds (多选监视项)
- [x] startTime / endTime (时间范围)
- [x] monitorStatusId (按 MonitorStatus 过滤) — 分布式模式通过 DB 查询实现
- [x] containNull (是否包含空状态)

验收 checklist：
- [ ] 选中一个监视项，事件列表按时间排序后，相邻行的 `occurMonitorStatusKey` 互不相同
- [ ] 有状态事件后有紧随的无状态事件（闭环）
- [ ] `containNull=false` 时不显示无状态事件

### 5.2 历史列表 (history-list)

URL: `http://127.0.0.1:5501/app/sis/monitor-center/history-list/`

**数据正确性规则:** 见 [4.2 历史会话完整性](#42-历史会话完整性)。每条历史记录代表一次完整的告警会话，包含开始时间、结束时间、持续时长和极值。

| 字段 | 说明 | 状态 |
|------|------|------|
| `startTime` | 告警触发时间（有状态事件发生时刻） | 已支持 |
| `endTime` | 告警消除时间（紧随的无状态事件发生时刻） | 已支持 — 事件流配对 |
| `Duration` | `endTime - startTime`，持续时长 | 已修复 — 负值守卫 |
| `maxValue` / `minValue` | 告警期间采集极值 | 已支持 |
| `monitorStatusDefName` | 状态名 | 已修复 — 从 DB 查询定义名称 |

检索条件验收：
- [x] filter / monitorItemIds / monitorStatusId / monitorTreeId
- [x] monitorStatusKey (状态键过滤)
- [x] startTime / endTime (时间范围)
- [x] containNull

验收 checklist：
- [ ] 每条记录的 `Duration` ≥ 0（无负时长）
- [ ] 同一监视项的历史记录按 `startTime` 升序排列，相邻记录时间不重叠
- [ ] 活跃告警（尚未消除）的 `endTime` 为空
- [ ] `maxValue` / `minValue` 反映告警期间的极值（不应始终等于 `triggerValue`）
- [ ] 每条历史记录可追溯到事件列表中对应的有状态事件 + 无状态事件配对

### 5.3 实时监视列表 (real-monitor-list)

URL: `http://127.0.0.1:5501/app/sis/monitor-center/real-monitor-list/`

| 修复项 | 状态 |
|--------|------|
| 本地模式分页 (Skip/Take) | 已修复 |
| 分布式模式分页 | 原本已正确 |

检索条件验收：
- [x] filter / monitorItemIds / monitorStatusId / showAll

---

## 七、多 Worker 运行验收

### 6.1 Worker 配置

`docker-compose.yml` 中 Worker Service 配置：
```yaml
deploy:
  replicas: 2
  resources:
    limits:
      cpus: "2"
      memory: "2G"
```

### 6.2 验证命令

```bash
# 查看 Worker 状态
docker compose ps

# 查看 Master 日志（含分区信息）
docker logs ruleengine-master --tail 20

# 查看 Worker 日志
docker compose logs -f worker
```

### 6.3 验收标准

- [x] 至少 2 个 Worker 实例运行
- [x] Master 日志显示分区分配信息（每个 Worker 分配监控项数量）
- [x] 不均衡度 < 10%
- [x] 缩容/扩容支持: `docker compose up -d --scale worker=5`
- [x] Worker 启动恢复: 从本地 StateStore 加载状态，缺失时从 ClickHouse 查询最后事件恢复 PreviousStatus

### 6.4 启动恢复流程

```
Worker 启动
  ├─ ① 注册到 Master
  ├─ ② 从本地 StateStore 批量加载状态
  └─ ③ 本地缺失 → ClickHouse 批量查最后事件:
       SELECT monitor_id, status_key
       FROM ruleengine.alarm_events
       WHERE monitor_id IN ({ids})
       ORDER BY occur_time DESC
       LIMIT 1 BY monitor_id
```

实现位置: `WorkerCalculationService.RunOneCycleAsync` (首次周期执行)
接口方法: `IAlarmWriter.GetLastEventStatusesAsync`
实现类: `ClickHouseAlarmWriter` (生产), `InMemoryAlarmWriter`/`RedisAlarmWriter` (返回空字典)

| 场景 | 恢复行为 |
|------|------|
| 正常重启 (StateStore 持久) | StateStore 有状态 → 直接使用 |
| Worker 漂移/首次启动 | StateStore 无状态 → ClickHouse 查询恢复 |
| 新监视项 (无历史事件) | ClickHouse 查询返回空 → PreviousStatus = "" |

---

## 八、数据准备验收

### 7.1 核心设计要求

**统一数据源：** 所有监视项和前置规则共用 TrendDB 测点 `db05.test1`，这是当前环境唯一具备实时值的测点。

**数据流：**
```
db05.test1 (TrendDB)
  → DataAcquisitionService (每秒采集)
  → TagValueStore (缓存实时值)
  → WorkerCalculationService (每秒计算)
     ├─ 前置规则评估: db05.test1 > prerule_threshold
     └─ 监视项计算:  db05.test1 > monitor_threshold (持续 N 秒)
```

**设计原则：**
- 所有监视项的 RealDB 数据源 (`source_flag=3`) 均指向 `db05.test1`
- 每个监视项通过不同的 Static 阈值 (`source_flag=1`) 实现差异化触发条件
- 每个监视项可配置不同 `duration_cnt` 实现错峰触发
- 前置规则也使用 `db05.test1`，在不同阈值条件下过滤监视项

### 7.2 监视项规模

| 指标 | 值 |
|------|-----|
| 总监视项数 | **≥ 10,000** |
| 前置规则数 | 2 (Expression A + RangeDuration B) |
| 前置规则分配 | 各关联约 5,000 个监视项 |
| 每个监视项数据源数 | 2 (1 RealDB + 1 Static) |
| 每个监视项规则数 | 1 (RangeDuration) |

### 7.3 数据库验证

```bash
# 验证监视项数量
docker exec sis-postgres psql -U postgres -d sis1 -c \
  "SELECT COUNT(*) AS monitors FROM ssmcitemmst WHERE enable_flag = true;"

# 验证数据源数量 (10,000 监视项 × 2 + 前置规则 × 2 = ~20,004)
docker exec sis-postgres psql -U postgres -d sis1 -c \
  "SELECT COUNT(*) AS sources FROM ssmcsourcemst WHERE valid_sta = 'A';"

# 验证规则数量 (10,000 监视项 × 1 + 前置规则 RangeDuration = ~10,001)
docker exec sis-postgres psql -U postgres -d sis1 -c \
  "SELECT COUNT(*) AS rules FROM ssmcrulerandurmst WHERE valid_sta = 'A';"

# 验证前置规则数量
docker exec sis-postgres psql -U postgres -d sis1 -c \
  "SELECT prerule_no, prerule_nam, rule_flag FROM ssmcprerulemst WHERE valid_sta = 'A';"
```

预期:
```
monitors: ≥ 10,000
sources:  ~20,004
rules:    ~10,001
prerules: 2
```

### 7.4 数据完整性验证

每个监视项应具备：
- [ ] 2 个数据源记录 (`ssmcsourcemst`): 1 个 RealDB 指向 `db05.test1` + 1 个 Static 阈值
- [ ] 1 个区间时长规则 (`ssmcrulerandurmst`)
- [ ] RuleType = 2 (RangeDuration)
- [ ] `prerule_no` 指向 TEST-PRERULE-A 或 TEST-PRERULE-B

```bash
# 检查每个监视项的数据源和规则是否完整
docker exec sis-postgres psql -U postgres -d sis1 -c "
SELECT i.monitor_no,
       (SELECT COUNT(*) FROM ssmcsourcemst s WHERE s.group_no = i.monitor_no AND s.valid_sta = 'A') as src_count,
       (SELECT COUNT(*) FROM ssmcrulerandurmst r WHERE r.related_no = i.monitor_no AND r.valid_sta = 'A') as rule_count
FROM ssmcitemmst i
WHERE i.enable_flag = true
AND (
    (SELECT COUNT(*) FROM ssmcsourcemst s WHERE s.group_no = i.monitor_no AND s.valid_sta = 'A') != 2
    OR (SELECT COUNT(*) FROM ssmcrulerandurmst r WHERE r.related_no = i.monitor_no AND r.valid_sta = 'A') != 1
);"
# 预期: 0 行 (无不完整监视项)
```

### 7.5 关键配置要求 (重要!)

**org_no 一致性：** `ssmcitemmst.org_no`、`ssmcsourcemst.org_no`、`ssmcrulerandurmst.org_no` 必须一致（统一使用 `-1`）。MonitorCenter 的 ABP 多租户过滤器 (`IMayHaveTenant`) 按 `org_no` 过滤，不一致会导致 `GetAllMonitors()` 查不到数据源和规则。

**表关联关系：**
```
ssmcitemmst.monitor_no ← ssmcsourcemst.group_no       (数据源关联)
ssmcitemmst.monitor_no ← ssmcrulerandurmst.related_no  (规则关联)
ssmcitemmst.source_no  → ssmcsourcemst.source_no       (关注数据源，为空字符串表示无特定关注)
```

**数据源要求 (每个监视项 2 个)：**

| source_flag | source_id | source_cod | 说明 |
|-------------|-----------|------------|------|
| 3 (TrendDB) | `tag_value` | `db05.test1` | 实时趋势测点 (统一) |
| 1 (Static) | `threshold` | `30` ~ `90` | 阈值常量 (每个监视项不同，实现差异化触发) |

**规则要求 (每个监视项 1 个 RangeDuration)：**

| 字段 | 值 |
|------|-----|
| `left_id` | `tag_value` (匹配 TrendDB 数据源 source_id) |
| `right_id` | `threshold` (匹配阈值数据源 source_id) |
| `symbol_flag` | 1 (大于) |
| `duration_cnt` | 1 ~ 60 (秒，每个监视项不同，实现错峰触发) |
| `statuslin_cod` | `satisfiled` |
| `status_no` | `88a2ff2f827f487e83bda61736ca3ad1` (考核条件主题) |

**前置规则数据源要求 (每个前置规则 2 个)：**

| 前置规则 | source_flag | source_id | source_cod | 说明 |
|----------|-------------|-----------|------------|------|
| A (Expression) | 3 (TrendDB) | `prerule_val` | `db05.test1` | 实时趋势测点 |
| A (Expression) | 1 (Static) | `prerule_thr` | `50` | 阈值常量 |
| B (RangeDuration) | 3 (TrendDB) | `prerule_val` | `db05.test1` | 实时趋势测点 |
| B (RangeDuration) | 1 (Static) | `prerule_thr` | `50` | 阈值常量 |

**前置规则表达式 (A):** `prerule_val > prerule_thr`
**前置规则 RangeDuration (B):** `prerule_val >= prerule_thr`, `duration_cnt = 0` (立即生效)

### 7.6 数据验证命令

```bash
# 检查 org_no 一致性
docker exec sis-postgres psql -U postgres -d sis1 -c "
SELECT 'ssmcitemmst' AS tbl, org_no, COUNT(*) FROM ssmcitemmst WHERE enable_flag = true GROUP BY org_no
UNION ALL
SELECT 'ssmcsourcemst', org_no, COUNT(*) FROM ssmcsourcemst WHERE valid_sta = 'A' GROUP BY org_no
UNION ALL
SELECT 'ssmcrulerandurmst', org_no, COUNT(*) FROM ssmcrulerandurmst WHERE valid_sta = 'A' GROUP BY org_no;"

# 验证所有监视项都使用 db05.test1
docker exec sis-postgres psql -U postgres -d sis1 -c "
SELECT s.source_cod, COUNT(*) as cnt
FROM ssmcsourcemst s
JOIN ssmcitemmst i ON s.group_no = i.monitor_no
WHERE i.enable_flag = true AND s.source_flag = 3 AND s.valid_sta = 'A'
GROUP BY s.source_cod;"
# 预期: 所有 source_flag=3 的数据源 source_cod 均为 db05.test1

# 验证前置规则数据源
docker exec sis-postgres psql -U postgres -d sis1 -c "
SELECT s.group_no, s.source_id, s.source_flag, s.source_cod
FROM ssmcsourcemst s
WHERE s.group_no LIKE 'TEST-PRERULE%' AND s.valid_sta = 'A'
ORDER BY s.group_no, s.source_flag;"
```

---

## 九、运行报告模板

### 8.1 服务健康检查

| 容器 | 状态 | 端口 |
|------|------|------|
| ruleengine-redis | `docker ps --filter name=ruleengine-redis` | 6379 |
| ruleengine-clickhouse | `docker ps --filter name=ruleengine-clickhouse` | 8123, 9000 |
| ruleengine-master | `docker ps --filter name=ruleengine-master` | 11082, 11083 |
| ruleengine-worker-N | `docker ps --filter name=ruleengine-worker` | 50051+ |

### 8.2 配置同步

```bash
# 通知变更
curl -X POST http://localhost:11082/api/ruleengine/monitors/on-changed \
  -H "Content-Type: application/json" \
  -d '{"added":[],"modified":[],"deleted":[],"version":"2026-07-19T00:00:00Z"}'
```

预期响应: `{"success": true, "totalMonitors": 10000}`

### 8.3 Worker 分区

Master 日志示例：
```
分区完成: 2 Worker, 10,000 监控项, 不均衡度 2.0%
Worker xxx: 5,000 监控项
Worker yyy: 5,000 监控项
```

### 8.4 前置规则加载

Master 日志示例：
```
数据库 fallback 加载前置规则: 2 条
Worker xxx 前置规则更新: 2 条
```

### 8.5 E2E 集成预期统计 (10,000 监视项, 10 分钟)

| 指标 | 预期值 |
|------|--------|
| ClickHouse 总事件 | ≥ 30,000 |
| 触发事件 (StatusKey != "") | ≥ 15,000 |
| 消除事件 (StatusKey = "") | ≥ 15,000 |
| Redis 活跃报警 | ≥ 3,000 |
| 产生事件的监视项比例 | ≥ 90% |
| Worker 分布 | 2 × 5,000 监视项 |

### 8.6 数据正确性自检

部署 10 分钟后执行以下查询，确保计算引擎行为正确：

```sql
-- 检查 1: 事件交替 — 不应出现相邻相同状态 (预期: 0 行)
-- 注意: 排除每个 partition 的第一行，避免 lagInFrame 默认值 "" 与空状态事件误匹配
SELECT count() AS consecutive_duplicates
FROM (
    SELECT monitor_id, status_key,
           lagInFrame(status_key) OVER (PARTITION BY monitor_id ORDER BY occur_time) AS prev_key,
           row_number() OVER (PARTITION BY monitor_id ORDER BY occur_time) AS rn
    FROM ruleengine.alarm_events
    WHERE occur_time >= now() - INTERVAL 10 MINUTE
)
WHERE rn > 1 AND status_key = prev_key;

-- 检查 2: 负时长 — 不应出现 endTime < startTime (预期: 0 行)
-- 注意: leadInFrame 对 partition 最后一行返回 epoch 0 (1970-01-01)，需过滤
SELECT count() AS negative_duration
FROM (
    SELECT monitor_id,
           leadInFrame(occur_time) OVER (PARTITION BY monitor_id ORDER BY occur_time) AS end_time,
           occur_time AS start_time, status_key
    FROM ruleengine.alarm_events
    WHERE occur_time >= now() - INTERVAL 10 MINUTE
)
WHERE status_key != ''
  AND end_time IS NOT NULL
  AND end_time > '2000-01-01'
  AND end_time < start_time;
```

**预期结果:** 两项检查均返回 0。

---

## 十、已修复缺陷清单

| # | 缺陷 | 修复方式 |
|---|------|----------|
| D1 | Worker 漂移后 PreviousStatus 丢失导致重复事件 | Worker 启动恢复: StateStore 缺失时从 ClickHouse 查询最后事件恢复状态 |
| D2 | 事件流模型: ALTER TABLE UPDATE 阻塞 | 移除 ALTER TABLE UPDATE，改用 LEAD 窗口函数查询侧配对 |
| D7 | containNull=true 时不返回空状态事件 | HistoryAlarmService SQL 去掉硬编码 `status_key != ''`，改为根据 containNull 参数动态添加 |
| A3 | RelatedStatusName 显示事件状态名 | 分布式模式从 MonitorStatus 表查询 Name |
| A4 | OccurMonitorStatusName 为空 | 分布式模式从 MonitorStatusDefinition 表查询 Name |
| A5 | totalCount 与实际不一致 | 传递 `monitorKeys` 到 Rule Engine 查询 |
| B1 | 持续时长可能为负数 | MonitorCenter 侧负值守卫 (`endTimeMs < startTimeMs` → null) |
| B3 | totalCount 与实际不一致 | 传递 `monitorKeys` 到 Rule Engine 查询 |
| C1 | 本地模式分页未生效 | `GetAllForList` 和 `GetAllKeysForList` 添加 Skip/Take |
| A1/A2 | lastEventName/unit 等字段为空 | ClickHouse schema + Model + DTO + Writer + Service 全链路添加新字段 |
| F1 | RangeDuration BreakOnHit=false 始终返回 false | `PreruleEvaluationService` 添加 `anyHit` 标志 |
| F2 | 前置规则评估无日志 | `EvaluateAllAsync` 添加 per-rule 结果日志 |
| F3 | 使用 Key 而非 SourceKey 获取 RealDB 数据 | 分离 Static(SourceType=1) 和 RealDB(SourceType=3) 处理 |
| F4 | Redis 实时报警重复写入 | 状态变更判断内部才写入 Redis |
| F5 | 前置规则 suppress 路径重复清除报警 | `ClearRealtimeAlarmAsync` 移入 PreviousStatus 非空检查内部 |
| F6 | MonitorCenter 全表扫描 | 分页+JOIN 查询，Master 分批拉取 |
| G1 | MonitorCenter GetAllPrerules API 404 | `PreruleDatabaseReader` Npgsql 直读 PostgreSQL fallback |
| G2 | Admin API load-prerules 未推送到 Workers | 注入分区服务，加载后调用 `PushToWorkersAsync` |
| G3 | gRPC 重连日志噪音 | HTTP/2 reset 场景降级为 `LogInformation` |
| H1 | Worker 重启后事件交替不变量被破坏 (状态恢复失败) | RedisAlarmWriter: Hash 过期时清理残留 SET 条目并留给 ClickHouse 回退; ProductionAlarmWriter: Redis 恢复缺失时自动回退 ClickHouse |
| H2 | ClickHouse `lagInFrame` 默认值 "" 与空状态事件误匹配 | 验收文档 SQL 增加 `WHERE rn > 1` 排除 partition 第一行 |
| H3 | 验收文档 SQL `leadInFrame` epoch 0 误判为负时长 | 增加 `end_time > '2000-01-01'` 过滤 |
| H4 | `lastEventName` 仅在 clear 事件上填充 | Worker 恢复时设置 `PreviousEventId = "recovered"`; 当 previous 为空时 lastEventName 输出 "normal" |
| H5 | `RealMonitorAppService` NRE 风险 (`MonitorItemIds.Count`) | 本地/分布式两处改为 `input.MonitorItemIds?.Count > 0` |
| H6 | Redis `active_alarms` SET 残留过期条目 | Hash 过期时自动 `SetRemoveAsync` 清理 |

---

## 十一、架构改动清单

### 10.1 Rule Engine (`03-Luculent.Sis.RuleEngine`)

| 文件 | 改动 |
|------|------|
| `Worker/WorkerCalculationService.cs` | HashSet 替换 List (O(n²)→O(1)); Parallel.ForEachAsync; 批量状态读写; 事件流模型; Worker 启动恢复; 前置规则 Pipeline 集成 |
| `Worker/Calculation/PreruleEvaluationService.cs` | 前置规则评估: Expression + RangeDuration; BreakOnHit 修复; SourceKey 处理; 日志 |
| `Worker/Calculation/PrerulePipeline.cs` | ManualFlag/StopMonitor/SourceDependency 抑制检查 |
| `Worker/Storage/ClickHouseAlarmWriter.cs` | 移除 ALTER TABLE UPDATE; 新增 `GetLastEventStatusesAsync` 启动恢复查询; lastEventId/Name/unit/jobId 字段 |
| `Worker/Storage/InMemoryAlarmWriter.cs` | 新增 `Clear()` 方法; 实现 `GetLastEventStatusesAsync` |
| `Worker/Storage/RedisAlarmWriter.cs` | 实现 `GetLastEventStatusesAsync` (返回空); 去重写入 |
| `Worker/Storage/ProductionAlarmWriter.cs` | 委托 `GetLastEventStatusesAsync` 到 `_history` |
| `Worker/Services/GrpcConnectionService.cs` | HTTP/2 reset 日志降噪; 配置推送包含前置规则 |
| `Shared/Interfaces/IAlarmWriter.cs` | 新增 `GetLastEventStatusesAsync` 方法 |
| `Shared/Models/AlarmModels.cs` | AlarmEvent 新增 LastEventId/Name/Unit/JobId/RelatedTriggerOccurTime |
| `Shared/DTOs/AlarmDTOs.cs` | `AlarmQueryRequest` 新增 `ContainNull`; AlarmEventDTO 新增 4 字段 |
| `Master/Program.cs` | 启动拉取全景配置; 前置规则 fallback; Admin API; 死 Worker 清理; 安全网重分区 |
| `Master/Services/PreruleDatabaseReader.cs` | **新增** — Npgsql 直读 PostgreSQL 前置规则定义 |
| `Master/Services/HistoryAlarmService.cs` | SQL 改为 LEAD 窗口函数配对; 支持 monitorKeys 过滤; containNull 动态控制 |
| `Master/Services/MonitorCenterClient.cs` | 分页批量拉取监视项和前置规则 |
| `deploy/clickhouse/init.sql` | 移除 event_type/clear_time; 新增 last_event_id/name/unit/job_id; 新 ORDER BY; TTL |
| `deploy/test-data-1000-monitors.sql` | 1000 监视项 + 2 前置规则测试数据 |

### 10.2 Monitor Center (`00-Luculent.Sis.MonitorCenter`)

| 文件 | 改动 |
|------|------|
| `Services/HistoryMonitorAppService.cs` | 分布式模式: 传递 monitorKeys; 负时长守卫; 查询 MonitorStatus/MonitorStatusDefinition; 使用 totalCount |
| `Services/EvnetDataAppService.cs` | 分布式模式: RelatedStatusName 从 DB 查询; OccurMonitorStatusName 从 DB 查询; 映射 lastEventId/Name/unit/jobId |
| `Services/RealMonitorAppService.cs` | 本地/分布式模式均添加 Skip/Take 分页 |
| `Services/RuleEngineClient.cs` | HistoryAlarmItem 添加 lastEventId/lastEventName/unit/jobId; 新增 containNull 参数 |
| `Services/MonitorDataForPublicAppService.cs` | `GetAllMonitors`/`GetAllPrerules` 分页+JOIN 避免全表扫描 |

---

## 十二、待完成项

### 11.1 待执行 — 测试数据重建

项目功能已就绪，以下只是重新生成测试数据并验证：

- [ ] **编写 10,000 监视项 SQL 脚本** — 所有监视项使用 `db05.test1` 作为统一数据源，阈值差异化 (30~90)，duration 差异化 (1~60s)，前置规则也改用 `db05.test1`
- [ ] **E2E 集成验证** — 部署 10,000 监视项后运行 10 分钟，验证 ClickHouse 事件数、Redis 活跃报警数、闭环完整性

### 11.2 代码完善

- [ ] D4: 添加报警流程集成测试 (`ProcessMonitorAsync` → `ClickHouseAlarmWriter` → `HistoryAlarmService` 端到端)
- [ ] D3: `AccumulatedDurationSec` 写入但从未读取 — 统一使用 `LastSatisfiedTimeMs` 计算，移除冗余字段
- [ ] 性能测试改为连接真实 TrendDB 数据源 (`db05.test1`)，替代模拟随机值

### 11.3 低优先级

- [ ] CI 添加 E2E 测试 stage (目前仅 `dotnet test`)
- [ ] ClickHouse 性能调优 (高负载下 CPU 优化)
