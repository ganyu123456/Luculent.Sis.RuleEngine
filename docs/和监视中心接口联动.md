规则引擎和监视中心00-Luculent.Sis.MonitorCenter的接口联动要求如下：
1. 若监视中心新增监视项目、更改监视项、删除监视项目等监视项发生变化时需要主动推送给规则引擎，这里我初步的想法是通过Http请求的方式；
2. 规则引擎需要能获取到全量的监视中心的所有监视项，所以监视中心应该提供这个接口；
3. 监视中心对外暴露的查询监视项实时报警和一段时间的历史值接口需要改造成直接调用规则引擎的查询历史的接口，但是这里得兼容老版本的查询关系库的功能，因此你得根据是否启用规则引擎来区分；
4. 当启动规则引擎时，监视中心的所有计算引擎都得停止，计算所有内容都交给规则引擎去做；

未完成事项
1. 你好像并没有实现参照/Users/ganyu/Desktop/myself/01-个人/个人/12-kubesphere/03-kubeedge/01-工业云边协同平台/01-code/15-write-to-trenddb-github/TrendDb的方法，主动实现获取时序库的实时值，无需再依赖别的模块，直接dll实现规则引擎获取时序库测点实时值的方法；
2. 你的03-Luculent.Sis.RuleEngine/src/Luculent.Sis.RuleEngine.Worker核心计算引擎好像还没有都实现吧，你的实现逻辑是什么呢，我没看明白；
3. 